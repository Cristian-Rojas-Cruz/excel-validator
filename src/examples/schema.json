[
  {
    "tabname": "MainData",
    "required": true,
    "minRows": 3,
    "columns": [
      { "name": "ID", "outputKey":"id", "required": true, "type": "string" },
      { "name": "Email", "outputKey":"email", "required": true, "type": "email" },
      { "name": "Category", "outputKey":"cat", "required": true, "type": "enum", "allowedValues": ["A", "B", "C"] },
      { "name": "Amount", "outputKey":"amount", "required": false, "type": "number" },
      { "name": "IsActive", "outputKey":"active", "required": false, "type": "boolean" },
      { "name": "ConditionalField", "outputKey":"condition", "required": false, "type": "string" },
      { "name": "Exclusive1", "outputKey":"exclusiveone", "required": false, "type": "string" },
      { "name": "Exclusive2", "outputKey":"exclusivetwo", "required": false, "type": "string" }
    ],
    "rules": {
      "unique": { "columns": ["ID", "Email"] },
      "mutuallyExclusive": { "columns": ["Exclusive1", "Exclusive2"] },
      "conditionalRequired": {
        "when": { "column": "Category", "equals": "C" },
        "thenRequired": ["ConditionalField"]
      }
    }
  },
  {
    "tabname": "OptionalSheet",
    "required": false,
    "minRows": 1,
    "columns": [
      { "name": "CustomField", "required": true, "type": "string" }
    ],
    "rules": {
      "customRule": { "exampleParam": true }
    }
  }
]